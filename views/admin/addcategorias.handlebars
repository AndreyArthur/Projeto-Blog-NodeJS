{{#each erros}}
  <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<div style="padding: 5% 10%;" class="card">
  <h2 class="mb-3">Nova Categoria:</h2>
  <form action="/admin/categorias/new" method="POST">
    <label for="nome">Nome: </label>
    <input class="form-control"  type="text" id="nome" name="nome" placeholder="Nome da categoria..."/>
    <label class="mt-1" for="slug">Slug: </label>
    <input class="form-control"  type="text" id="slug" name="slug" placeholder="Slug da categoria..."/>
    <button type="submit" class="btn btn-success mt-3">Criar Categoria</button>
  </form>
</div><!--card-->